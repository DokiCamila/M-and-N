<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Class="ImageCropper.UcImageCropper" Height="113" Width="120"
    >
    <Grid x:Name="gridMain">

        <Grid.Resources>
            <ResourceDictionary>

                <Style x:Key="defaultCropperStyle" TargetType="{x:Type Rectangle}">
                    <Setter Property="Stroke" Value="Blue"/>
                    <Setter Property="Fill" Value="#FF105691"/>
                    <Setter Property="Opacity" Value="0.7"/>
                </Style>

                <!--<ResourceDictionary.MergedDictionaries>
          <ResourceDictionary Source="Resources\AppStyles.xaml"/>
        </ResourceDictionary.MergedDictionaries>-->
            </ResourceDictionary>

        </Grid.Resources>

        <Border CornerRadius="5,5,5,5" Background="White" DockPanel.Dock="Top" Margin="10" >
            <ScrollViewer  x:Name="svForImg"  Margin="7"   ScrollViewer.CanContentScroll="True" ScrollViewer.HorizontalScrollBarVisibility="Visible"  ScrollViewer.VerticalScrollBarVisibility="Visible" Visibility="Visible">
            </ScrollViewer>
        </Border>

    </Grid>
</UserControl>
